# 系统设置数据交互移植 - 技术方案

## 1. 技术方案概述

本次改动目标是将 Vue 版本 settings.vue 中完整的后端数据交互逻辑移植到 React 版本 Settings.tsx，实现：
- 页面初始化从后端加载配置
- 保存设置到后端数据库持久化
- AI 配置动态增删改
- 条件显示逻辑
- 赞助码验证
- 导入导出功能改造

改动范围仅限 Settings.tsx 文件，保持现有 Tailwind CSS 视觉风格不变。

## 2. 需求依据

需求文档路径：`/Users/daxue_1/ai-workspace/go-stock/feature/需求-系统设置数据交互移植-20260319.md`

## 3. 涉及文件

### 3.1 需要修改的文件

| 文件路径 | 改动说明 |
|---------|---------|
| frontend/src/app/components/Settings.tsx | 主要改动文件，重构 state、添加 API 调用、实现完整数据交互 |

### 3.2 参考文件

| 文件路径 | 参考说明 |
|---------|---------|
| frontend_vue/src/components/settings.vue | Vue 版本完整实现，作为逻辑移植参考 |
| frontend/wailsjs/go/main/App.d.ts | API 类型声明 |
| frontend/wailsjs/go/main/App.js | API 调用入口 |
| frontend/wailsjs/go/models.ts | SettingConfig、AIConfig 类型定义 |

## 4. 架构与模块设计

### 4.1 Settings.tsx 整体架构改造

```
Settings.tsx
├── imports（新增 wailsjs API 和类型导入）
├── State 层（重构为对齐后端 SettingConfig 模型）
├── 初始化层（useEffect 调用 GetConfig）
├── 事件处理层
│   ├── handleSave（调用 UpdateConfig）
│   ├── handleExport（调用 ExportConfig）
│   ├── handleImport（读取文件 + UpdateConfig）
│   ├── handleVerifySponsorCode（调用 CheckSponsorCode）
│   ├── addAiConfig（添加 AI 配置）
│   ├── removeAiConfig（删除 AI 配置）
│   └── sendTestNotice（发送测试通知）
├── UI 组件层
│   ├── ToggleSwitch（复用）
│   ├── 基础设置区域（补全字段）
│   ├── 通知设置区域（补全字段 + 条件显示）
│   └── AI 设置区域（条件显示 + 动态配置卡片）
└── 操作按钮区域
```

### 4.2 State 结构重新设计

State 将对齐后端 SettingConfig 模型，不再使用分散的独立 state 变量，改用单一配置对象 + 部分独立状态。

### 4.3 API 集成层设计

```typescript
// 导入 API
import { 
  GetConfig, 
  UpdateConfig, 
  ExportConfig, 
  CheckSponsorCode,
  SendDingDingMessageByType 
} from '../../wailsjs/go/main/App';

// 导入类型
import { data } from '../../wailsjs/go/models';
```

## 5. 数据模型与接口协议

### 5.1 SettingConfig 完整类型定义（来自 models.ts）

```typescript
export class SettingConfig {
  ID: number;
  CreatedAt: any;
  UpdatedAt: any;
  DeletedAt: any;
  tushareToken: string;
  localPushEnable: boolean;
  dingPushEnable: boolean;
  dingRobot: string;
  updateBasicInfoOnStart: boolean;
  refreshInterval: number;
  openAiEnable: boolean;
  prompt: string;
  checkUpdate: boolean;
  questionTemplate: string;
  crawlTimeOut: number;
  kDays: number;
  enableDanmu: boolean;
  browserPath: string;
  enableNews: boolean;
  darkTheme: boolean;
  browserPoolSize: number;
  enableFund: boolean;
  enablePushNews: boolean;
  enableOnlyPushRedNews: boolean;
  sponsorCode: string;
  httpProxy: string;
  httpProxyEnabled: boolean;
  enableAgent: boolean;
  qgqpBId: string;
  aiConfigs: AIConfig[];
}
```

### 5.2 AIConfig 完整类型定义（来自 models.ts）

```typescript
export class AIConfig {
  ID: number;
  CreatedAt: any;
  UpdatedAt: any;
  name: string;
  baseUrl: string;
  apiKey: string;
  modelName: string;
  maxTokens: number;
  temperature: number;
  timeOut: number;
  httpProxy: string;
  httpProxyEnabled: boolean;
}
```

### 5.3 API 接口说明

| API | 参数 | 返回值 | 说明 |
|-----|-----|-------|------|
| GetConfig() | 无 | Promise<data.SettingConfig> | 获取当前配置 |
| UpdateConfig(config) | data.SettingConfig | Promise<string> | 保存配置，返回成功/失败消息 |
| ExportConfig() | 无 | Promise<string> | 导出配置到文件，返回操作结果 |
| CheckSponsorCode(code) | string | Promise<{code: boolean, msg: string}> | 验证赞助码 |
| SendDingDingMessageByType(msg, id, type) | string, string, number | Promise<string> | 发送钉钉测试通知 |

## 6. 代码级设计（核心）

### 6.1 State 设计

#### 当前 React 的 state（需要删除/替换）

```typescript
// 以下 state 需要删除
const [tushareToken, setTushareToken] = useState('');
const [browserPath, setBrowserPath] = useState('...');
const [eastMoneyId, setEastMoneyId] = useState('');
const [purchaseCode, setPurchaseCode] = useState('');
const [autoUpdate, setAutoUpdate] = useState(false);
const [validateClipboard, setValidateClipboard] = useState(false); // 删除，伪字段
const [fillTheme, setFillTheme] = useState(false); // 删除，伪字段
const [indexFund, setIndexFund] = useState(false);
const [aiLibrary, setAiLibrary] = useState(false); // 删除，伪字段
const [subscribeNotify, setSubscribeNotify] = useState(false); // 删除，伪字段
const [localNotify, setLocalNotify] = useState(false);
const [alertFunction, setAlertFunction] = useState(false); // 删除，伪字段
const [showFlashNews, setShowFlashNews] = useState(false);
const [marketNewsAlert, setMarketNewsAlert] = useState(false);
const [aiDetection, setAiDetection] = useState(false);
const [crawlerNotify, setCrawlerNotify] = useState(false); // 删除，伪字段
```

#### 新的 state 设计

```typescript
import { useState, useEffect } from 'react';
import { data } from '../../wailsjs/go/models';

// 基础设置
const [configId, setConfigId] = useState<number>(1);
const [tushareToken, setTushareToken] = useState('');
const [updateBasicInfoOnStart, setUpdateBasicInfoOnStart] = useState(false);
const [refreshInterval, setRefreshInterval] = useState(1);
const [darkTheme, setDarkTheme] = useState(true);
const [browserPath, setBrowserPath] = useState('');
const [enableFund, setEnableFund] = useState(false);
const [enableAgent, setEnableAgent] = useState(false);
const [qgqpBId, setQgqpBId] = useState('');
const [sponsorCode, setSponsorCode] = useState('');

// 通知设置
const [dingPushEnable, setDingPushEnable] = useState(false);
const [dingRobot, setDingRobot] = useState('');
const [localPushEnable, setLocalPushEnable] = useState(true);
const [enableDanmu, setEnableDanmu] = useState(false);
const [enableNews, setEnableNews] = useState(false);
const [enablePushNews, setEnablePushNews] = useState(true);
const [enableOnlyPushRedNews, setEnableOnlyPushRedNews] = useState(true);

// AI 设置
const [openAiEnable, setOpenAiEnable] = useState(false);
const [crawlTimeOut, setCrawlTimeOut] = useState(30);
const [kDays, setKDays] = useState(30);
const [httpProxyEnabled, setHttpProxyEnabled] = useState(false);
const [httpProxy, setHttpProxy] = useState('');
const [prompt, setPrompt] = useState('');
const [questionTemplate, setQuestionTemplate] = useState('{{stockName}}分析和总结');
const [aiConfigs, setAiConfigs] = useState<data.AIConfig[]>([]);

// UI 状态
const [loading, setLoading] = useState(false);
const [message, setMessage] = useState<{type: 'success' | 'error' | 'info', text: string} | null>(null);
```

### 6.2 初始化加载（useEffect）

```typescript
useEffect(() => {
  loadConfig();
}, []);

const loadConfig = async () => {
  try {
    setLoading(true);
    const res = await GetConfig();
    
    // 基础设置映射
    setConfigId(res.ID);
    setTushareToken(res.tushareToken || '');
    setUpdateBasicInfoOnStart(res.updateBasicInfoOnStart || false);
    setRefreshInterval(res.refreshInterval || 1);
    setDarkTheme(res.darkTheme ?? true);
    setBrowserPath(res.browserPath || '');
    setEnableFund(res.enableFund || false);
    setEnableAgent(res.enableAgent || false);
    setQgqpBId(res.qgqpBId || '');
    setSponsorCode(res.sponsorCode || '');
    
    // 通知设置映射
    setDingPushEnable(res.dingPushEnable || false);
    setDingRobot(res.dingRobot || '');
    setLocalPushEnable(res.localPushEnable ?? true);
    setEnableDanmu(res.enableDanmu || false);
    setEnableNews(res.enableNews || false);
    setEnablePushNews(res.enablePushNews ?? true);
    setEnableOnlyPushRedNews(res.enableOnlyPushRedNews ?? true);
    
    // AI 设置映射
    setOpenAiEnable(res.openAiEnable || false);
    setCrawlTimeOut(res.crawlTimeOut || 30);
    setKDays(res.kDays || 30);
    setHttpProxyEnabled(res.httpProxyEnabled || false);
    setHttpProxy(res.httpProxy || '');
    setPrompt(res.prompt || '');
    setQuestionTemplate(res.questionTemplate || '{{stockName}}分析和总结');
    setAiConfigs(res.aiConfigs || []);
    
  } catch (error) {
    console.error('加载配置失败:', error);
    showMessage('error', '加载配置失败');
  } finally {
    setLoading(false);
  }
};

const showMessage = (type: 'success' | 'error' | 'info', text: string) => {
  setMessage({ type, text });
  setTimeout(() => setMessage(null), 3000);
};
```

### 6.3 保存设置（handleSave）

```typescript
const handleSave = async () => {
  try {
    setLoading(true);
    
    // 构建 SettingConfig 对象
    const config = new data.SettingConfig({
      ID: configId,
      tushareToken,
      localPushEnable,
      dingPushEnable,
      dingRobot,
      updateBasicInfoOnStart,
      refreshInterval,
      openAiEnable,
      prompt,
      questionTemplate,
      crawlTimeOut,
      kDays,
      enableDanmu,
      browserPath,
      enableNews,
      darkTheme,
      enableFund,
      enablePushNews,
      enableOnlyPushRedNews,
      sponsorCode,
      httpProxy,
      httpProxyEnabled,
      enableAgent,
      qgqpBId,
      aiConfigs,
    });
    
    // 如果有赞助码，先验证
    if (sponsorCode) {
      const verifyResult = await CheckSponsorCode(sponsorCode);
      if (!verifyResult.code) {
        showMessage('error', verifyResult.msg);
        return;
      }
    }
    
    // 调用更新 API
    const result = await UpdateConfig(config);
    showMessage('success', result);
    
  } catch (error) {
    console.error('保存配置失败:', error);
    showMessage('error', '保存配置失败');
  } finally {
    setLoading(false);
  }
};
```

### 6.4 AI 配置动态管理

#### addAiConfig 函数设计

```typescript
const addAiConfig = () => {
  const newConfig = new data.AIConfig({
    ID: 0,
    name: '',
    baseUrl: 'https://api.deepseek.com',
    apiKey: '',
    modelName: 'deepseek-reasoner',
    temperature: 0.1,
    maxTokens: 8192,
    timeOut: 6000,
    httpProxy: '',
    httpProxyEnabled: false,
  });
  setAiConfigs([...aiConfigs, newConfig]);
};
```

#### removeAiConfig 函数设计

```typescript
const removeAiConfig = (index: number) => {
  setAiConfigs(aiConfigs.filter((_, i) => i !== index));
};
```

#### 更新单个 AI 配置字段

```typescript
const updateAiConfig = (index: number, field: keyof data.AIConfig, value: any) => {
  const newConfigs = [...aiConfigs];
  newConfigs[index] = { ...newConfigs[index], [field]: value };
  setAiConfigs(newConfigs);
};
```

#### AI 配置卡片 JSX 渲染

```tsx
{openAiEnable && (
  <div className="space-y-4">
    {aiConfigs.map((aiConfig, index) => (
      <div key={index} className="bg-slate-800/40 rounded-lg border border-white/10 p-4">
        {/* 卡片头部 */}
        <div className="flex items-center justify-between mb-4">
          <span className="text-sm text-gray-400">AI 配置 #{index + 1}</span>
          <button
            onClick={() => removeAiConfig(index)}
            className="px-3 py-1 bg-red-500/20 border border-red-500/30 rounded text-xs text-red-400 hover:bg-red-500/30"
          >
            删除
          </button>
        </div>
        
        {/* 配置字段 */}
        <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label className="block text-sm text-gray-300 mb-2">配置名称:</label>
            <input
              type="text"
              value={aiConfig.name || ''}
              onChange={(e) => updateAiConfig(index, 'name', e.target.value)}
              placeholder="配置名称"
              className="w-full px-3 py-2 bg-slate-800/60 border border-white/10 rounded-lg text-sm text-white"
            />
          </div>
          
          <div>
            <label className="block text-sm text-gray-300 mb-2">接口地址:</label>
            <input
              type="text"
              value={aiConfig.baseUrl || ''}
              onChange={(e) => updateAiConfig(index, 'baseUrl', e.target.value)}
              placeholder="AI接口地址"
              className="w-full px-3 py-2 bg-slate-800/60 border border-white/10 rounded-lg text-sm text-white"
            />
          </div>
          
          <div>
            <label className="block text-sm text-gray-300 mb-2">令牌(apiKey):</label>
            <input
              type="password"
              value={aiConfig.apiKey || ''}
              onChange={(e) => updateAiConfig(index, 'apiKey', e.target.value)}
              placeholder="apiKey"
              className="w-full px-3 py-2 bg-slate-800/60 border border-white/10 rounded-lg text-sm text-white"
            />
          </div>
          
          <div>
            <label className="block text-sm text-gray-300 mb-2">模型名称:</label>
            <input
              type="text"
              value={aiConfig.modelName || ''}
              onChange={(e) => updateAiConfig(index, 'modelName', e.target.value)}
              placeholder="AI模型名称"
              className="w-full px-3 py-2 bg-slate-800/60 border border-white/10 rounded-lg text-sm text-white"
            />
          </div>
          
          <div>
            <label className="block text-sm text-gray-300 mb-2">Temperature:</label>
            <input
              type="number"
              step="0.1"
              value={aiConfig.temperature ?? 0.1}
              onChange={(e) => updateAiConfig(index, 'temperature', parseFloat(e.target.value))}
              className="w-full px-3 py-2 bg-slate-800/60 border border-white/10 rounded-lg text-sm text-white"
            />
          </div>
          
          <div>
            <label className="block text-sm text-gray-300 mb-2">MaxTokens:</label>
            <input
              type="number"
              value={aiConfig.maxTokens ?? 8192}
              onChange={(e) => updateAiConfig(index, 'maxTokens', parseInt(e.target.value))}
              className="w-full px-3 py-2 bg-slate-800/60 border border-white/10 rounded-lg text-sm text-white"
            />
          </div>
          
          <div>
            <label className="block text-sm text-gray-300 mb-2">Timeout(秒):</label>
            <input
              type="number"
              min="60"
              value={aiConfig.timeOut ?? 6000}
              onChange={(e) => updateAiConfig(index, 'timeOut', parseInt(e.target.value))}
              className="w-full px-3 py-2 bg-slate-800/60 border border-white/10 rounded-lg text-sm text-white"
            />
          </div>
          
          <div className="flex items-end gap-4">
            <ToggleSwitch
              checked={aiConfig.httpProxyEnabled || false}
              onChange={(val) => updateAiConfig(index, 'httpProxyEnabled', val)}
              label="http代理"
            />
          </div>
          
          {aiConfig.httpProxyEnabled && (
            <div className="md:col-span-2">
              <label className="block text-sm text-gray-300 mb-2">http代理地址:</label>
              <input
                type="text"
                value={aiConfig.httpProxy || ''}
                onChange={(e) => updateAiConfig(index, 'httpProxy', e.target.value)}
                placeholder="http代理地址"
                className="w-full px-3 py-2 bg-slate-800/60 border border-white/10 rounded-lg text-sm text-white"
              />
            </div>
          )}
        </div>
      </div>
    ))}
    
    {/* 添加按钮 */}
    <button
      onClick={addAiConfig}
      className="w-full py-3 border-2 border-dashed border-cyan-500/30 rounded-lg text-cyan-400 hover:border-cyan-500/50 hover:bg-cyan-500/10 transition-all"
    >
      + 添加AI配置
    </button>
  </div>
)}
```

### 6.5 条件显示逻辑

| 条件 | 控制元素 | 实现方式 |
|------|---------|---------|
| openAiEnable | AI 设置区域所有内容（除总开关） | `{openAiEnable && (...)}` |
| dingPushEnable | 钉钉机器人地址输入框 + 测试按钮 | `{dingPushEnable && (...)}` |
| enablePushNews | 只提醒红字新闻开关 | `{enablePushNews && (...)}` |
| httpProxyEnabled（全局） | http代理地址输入框 | `{httpProxyEnabled && (...)}` |
| aiConfig.httpProxyEnabled | 每个 AI 配置的代理地址输入框 | `{aiConfig.httpProxyEnabled && (...)}` |

### 6.6 赞助码验证

```typescript
const handleVerifySponsorCode = async () => {
  if (!sponsorCode) {
    showMessage('info', '请输入赞助码');
    return;
  }
  
  try {
    const result = await CheckSponsorCode(sponsorCode);
    if (result.code) {
      showMessage('success', result.msg);
    } else {
      showMessage('error', result.msg);
    }
  } catch (error) {
    console.error('验证赞助码失败:', error);
    showMessage('error', '验证赞助码失败');
  }
};
```

### 6.7 导入导出改造

#### 导出改造（使用后端 API）

```typescript
const handleExport = async () => {
  try {
    const result = await ExportConfig();
    showMessage('info', result);
  } catch (error) {
    console.error('导出配置失败:', error);
    showMessage('error', '导出配置失败');
  }
};
```

#### 导入改造（读取文件后调用 UpdateConfig）

```typescript
const handleImport = () => {
  const input = document.createElement('input');
  input.type = 'file';
  input.accept = '.json';
  input.onchange = async (e) => {
    const file = (e.target as HTMLInputElement).files?.[0];
    if (file) {
      const reader = new FileReader();
      reader.onload = async (event) => {
        try {
          const config = JSON.parse(event.target?.result as string);
          
          // 映射到 state
          setConfigId(config.ID || 1);
          setTushareToken(config.tushareToken || '');
          setUpdateBasicInfoOnStart(config.updateBasicInfoOnStart || false);
          setRefreshInterval(config.refreshInterval || 1);
          setDarkTheme(config.darkTheme ?? true);
          setBrowserPath(config.browserPath || '');
          setEnableFund(config.enableFund || false);
          setEnableAgent(config.enableAgent || false);
          setQgqpBId(config.qgqpBId || '');
          setSponsorCode(config.sponsorCode || '');
          
          setDingPushEnable(config.dingPushEnable || false);
          setDingRobot(config.dingRobot || '');
          setLocalPushEnable(config.localPushEnable ?? true);
          setEnableDanmu(config.enableDanmu || false);
          setEnableNews(config.enableNews || false);
          setEnablePushNews(config.enablePushNews ?? true);
          setEnableOnlyPushRedNews(config.enableOnlyPushRedNews ?? true);
          
          setOpenAiEnable(config.openAiEnable || false);
          setCrawlTimeOut(config.crawlTimeOut || 30);
          setKDays(config.kDays || 30);
          setHttpProxyEnabled(config.httpProxyEnabled || false);
          setHttpProxy(config.httpProxy || '');
          setPrompt(config.prompt || '');
          setQuestionTemplate(config.questionTemplate || '{{stockName}}分析和总结');
          setAiConfigs(config.aiConfigs || []);
          
          showMessage('success', '配置导入成功，请点击保存按钮保存到后端');
        } catch (error) {
          console.error('配置文件解析失败:', error);
          showMessage('error', '配置文件解析失败');
        }
      };
      reader.readAsText(file);
    }
  };
  input.click();
};
```

### 6.8 发送测试通知

```typescript
const sendTestNotice = async () => {
  const markdown = "### 测试\n" + new Date();
  const msg = JSON.stringify({
    msgtype: "markdown",
    markdown: {
      title: new Date().toString(),
      text: markdown
    },
    at: {
      isAtAll: true
    }
  });

  try {
    const result = await SendDingDingMessageByType(msg, "test-" + new Date().getTime(), 1);
    showMessage('info', result);
  } catch (error) {
    console.error('发送测试通知失败:', error);
    showMessage('error', '发送测试通知失败');
  }
};
```

### 6.9 UI 改造清单

#### 需要新增的 UI 元素

| 字段 | UI 类型 | 位置 |
|------|--------|------|
| updateBasicInfoOnStart | ToggleSwitch | 基础设置 |
| refreshInterval | number input | 基础设置 |
| darkTheme | ToggleSwitch | 基础设置 |
| enableAgent | ToggleSwitch | 基础设置 |
| qgqpBId | text input | 基础设置 |
| dingPushEnable | ToggleSwitch | 通知设置 |
| dingRobot | text input (条件) | 通知设置 |
| enableDanmu | ToggleSwitch | 通知设置 |
| enableOnlyPushRedNews | ToggleSwitch (条件) | 通知设置 |
| 发送测试通知按钮 | button (条件) | 通知设置 |
| crawlTimeOut | number input | AI 设置 |
| kDays | number input | AI 设置 |
| httpProxyEnabled | ToggleSwitch | AI 设置 |
| httpProxy | text input (条件) | AI 设置 |
| prompt | textarea | AI 设置 |
| questionTemplate | textarea | AI 设置 |
| AI 配置卡片列表 | 动态组件 | AI 设置 |
| 添加 AI 配置按钮 | button | AI 设置 |

#### 需要删除的 UI 元素（伪字段）

| 字段 | 当前位置 |
|------|---------|
| validateClipboard | 基础设置 |
| fillTheme | 基础设置 |
| aiLibrary | 基础设置 |
| subscribeNotify | 通知设置 |
| alertFunction | 通知设置 |
| crawlerNotify | AI 设置 |

#### 需要修改的 UI 元素（字段名映射）

| 当前名称 | 修改为 | 后端字段 |
|---------|-------|---------|
| autoUpdate | updateBasicInfoOnStart | updateBasicInfoOnStart |
| indexFund | enableFund | enableFund |
| localNotify | localPushEnable | localPushEnable |
| showFlashNews | enableNews | enableNews |
| marketNewsAlert | enablePushNews | enablePushNews |
| aiDetection | openAiEnable | openAiEnable |
| eastMoneyId | qgqpBId | qgqpBId |
| purchaseCode | sponsorCode | sponsorCode |

## 7. 开发步骤

### 步骤 1：重构 state 结构，对齐后端模型

- 具体做什么：删除所有伪字段的 state，新增缺失字段的 state，添加 API 和类型导入
- 执行主体：AI Coding
- 涉及文件：frontend/src/app/components/Settings.tsx
- 完成标准：所有 useState 变量与 SettingConfig 模型字段一一对应，无多余无缺失

### 步骤 2：实现初始化加载（useEffect + GetConfig）

- 具体做什么：添加 useEffect 调用 GetConfig()，将响应数据映射到各 state
- 执行主体：AI Coding
- 涉及文件：frontend/src/app/components/Settings.tsx
- 完成标准：页面加载时自动从后端获取配置并填充到 UI

### 步骤 3：重写 handleSave（构建 SettingConfig + UpdateConfig API）

- 具体做什么：从 state 构建完整 SettingConfig 对象，调用 UpdateConfig 保存
- 执行主体：AI Coding
- 涉及文件：frontend/src/app/components/Settings.tsx
- 完成标准：点击保存按钮后配置持久化到后端，有成功/失败提示

### 步骤 4：实现 AI 配置动态管理（增删改 + 卡片渲染）

- 具体做什么：实现 addAiConfig、removeAiConfig、updateAiConfig 函数，渲染 AI 配置卡片列表
- 执行主体：AI Coding
- 涉及文件：frontend/src/app/components/Settings.tsx
- 完成标准：可以添加、删除、编辑 AI 配置，保存后不丢失

### 步骤 5：实现条件显示逻辑

- 具体做什么：根据各开关状态控制相关 UI 区域的显示隐藏
- 执行主体：AI Coding
- 涉及文件：frontend/src/app/components/Settings.tsx
- 完成标准：5 个条件显示逻辑全部正确工作

### 步骤 6：实现赞助码验证

- 具体做什么：添加验证按钮点击事件，调用 CheckSponsorCode API
- 执行主体：AI Coding
- 涉及文件：frontend/src/app/components/Settings.tsx
- 完成标准：点击验证按钮显示验证结果，保存时自动验证

### 步骤 7：改造导入导出功能

- 具体做什么：导出改为调用 ExportConfig()，导入改为读取文件后映射到 state
- 执行主体：AI Coding
- 涉及文件：frontend/src/app/components/Settings.tsx
- 完成标准：导出通过系统文件对话框保存，导入后数据正确显示

### 步骤 8：UI 字段补全与清理

- 具体做什么：新增缺失字段的 UI 组件，删除伪字段的 UI 组件，修正字段标签
- 执行主体：AI Coding
- 涉及文件：frontend/src/app/components/Settings.tsx
- 完成标准：UI 与后端模型完全对齐，无多余无缺失

### 步骤 9：集成测试验证

- 具体做什么：启动应用，完整测试所有功能点
- 执行主体：AI Coding
- 涉及文件：无
- 完成标准：
  1. 打开设置页面自动加载配置
  2. 修改设置保存后重启仍在
  3. AI 配置增删改正常
  4. 条件显示正确
  5. 导入导出正常
  6. 赞助码验证正常

## 8. 字段映射对照表

### Vue formValue 到 React state 映射

| Vue formValue 路径 | React state | 后端字段 |
|-------------------|-------------|---------|
| formValue.ID | configId | ID |
| formValue.tushareToken | tushareToken | tushareToken |
| formValue.dingPush.enable | dingPushEnable | dingPushEnable |
| formValue.dingPush.dingRobot | dingRobot | dingRobot |
| formValue.localPush.enable | localPushEnable | localPushEnable |
| formValue.updateBasicInfoOnStart | updateBasicInfoOnStart | updateBasicInfoOnStart |
| formValue.refreshInterval | refreshInterval | refreshInterval |
| formValue.openAI.enable | openAiEnable | openAiEnable |
| formValue.openAI.aiConfigs | aiConfigs | aiConfigs |
| formValue.openAI.prompt | prompt | prompt |
| formValue.openAI.questionTemplate | questionTemplate | questionTemplate |
| formValue.openAI.crawlTimeOut | crawlTimeOut | crawlTimeOut |
| formValue.openAI.kDays | kDays | kDays |
| formValue.enableDanmu | enableDanmu | enableDanmu |
| formValue.browserPath | browserPath | browserPath |
| formValue.enableNews | enableNews | enableNews |
| formValue.darkTheme | darkTheme | darkTheme |
| formValue.enableFund | enableFund | enableFund |
| formValue.enablePushNews | enablePushNews | enablePushNews |
| formValue.enableOnlyPushRedNews | enableOnlyPushRedNews | enableOnlyPushRedNews |
| formValue.sponsorCode | sponsorCode | sponsorCode |
| formValue.httpProxy | httpProxy | httpProxy |
| formValue.httpProxyEnabled | httpProxyEnabled | httpProxyEnabled |
| formValue.enableAgent | enableAgent | enableAgent |
| formValue.qgqpBId | qgqpBId | qgqpBId |
