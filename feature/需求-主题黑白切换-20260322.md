# 需求：主题黑白切换（与 `darkTheme` 统一）

## 场景

- **浅色**：白/近白主背景，前景与边框使用设计令牌，保证可读性。
- **深色**：保留蓝黑渐变氛围，与现有深色体验一致。

## 入口

- 设置页「暗黑主题」开关，持久化字段仍为 `SettingConfig.darkTheme`（与后端一致）。

## 与实现的关系

- 前端通过 `html` 根节点的 `dark` 类与 `theme.css` 令牌联动；图表与 Wails 窗口底色与 `isDark` 同步。
