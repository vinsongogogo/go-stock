# AI 分析决策仪表盘展示改造 - 技术方案

> **版本**: 1.1
> **日期**: 2026-03-20
> **需求文档**: [需求-AI分析决策仪表盘展示改造-20260320.md](./需求-AI分析决策仪表盘展示改造-20260320.md)
> **变更记录**: v1.1 补全 buildAnalysisQuestion 改造、Prompt 使用策略修正、renderDashboard 缺失展示模块、流式解析时机

---

## 1. 需求依据与改造范围

### 1.1 需求依据

本技术方案依据 `需求-AI分析决策仪表盘展示改造-20260320.md` 文档，核心目标是将 AI 股票分析结果从纯文本/Markdown 展示改造为结构化的**决策仪表盘**样式，参考 daily_stock_analysis 项目的实现方案。

### 1.2 改造范围

| 改造模块 | 改造内容 | 改造程度 |
|---------|---------|---------|
| 后端 Prompt | 新增仪表盘 SYSTEM_PROMPT，引导 AI 输出结构化 JSON | 新增 |
| 前端组件 | 新建 ScoreGauge、DashboardCard、StrategyPoints、ActionChecklist 组件 | 新建 |
| AIAnalysis.tsx | 新增仪表盘渲染逻辑，保留 Markdown 降级；改造 `buildAnalysisQuestion` 适配仪表盘 JSON 格式 | 修改 |
| 数据兼容 | JSON 检测、解析、降级策略 | 新增 |

### 1.3 不在范围内

- 不改变后端数据采集逻辑
- 不改变流式传输机制（EventsEmit + SSE）
- 不改变数据库表结构
- 不改变分析结果存储方式（AIResponseResult.Content）

---

## 2. 现有架构分析

### 2.1 当前数据流

```
前端请求 (NewChatStream)
    ↓
app.go (NewChatStream 方法)
    ↓
openai_api.go (NewChatStream 方法)
    ↓
AI 流式输出
    ↓
runtime.EventsEmit("newChatStream", msg)
    ↓
前端 EventsOn 接收
    ↓
AIAnalysis.tsx 渲染 + SaveAIResponseResult 保存
```

### 2.2 当前 Prompt 构建逻辑

在 `openai_api.go` 的 `NewChatStream` 方法中：

1. 通过 `sysPromptId` 参数获取系统 Prompt，若为空则使用 `o.Prompt`（来自 SettingConfig）
2. 系统 Prompt 存储在 `prompt_templates` 表中
3. 用户 Question 由前端 `buildAnalysisQuestion` 函数构建（AIAnalysis.tsx 行 255-311），包含 JSON 格式引导

**注意**：现有 `buildAnalysisQuestion` 中内嵌了旧版 JSON 格式引导（要求输出 `overallScore/recommendation/technicalScore` 等字段），与新的仪表盘 JSON 结构不同。如果只改后端系统 Prompt 而不改 `buildAnalysisQuestion`，会导致系统 Prompt 与用户 Question 的格式要求冲突，AI 输出格式不确定。**必须同步改造 `buildAnalysisQuestion`**。

### 2.3 当前前端展示逻辑

`AIAnalysis.tsx` 中的展示逻辑：

1. **parseStructuredData**: 从 `Content` 中提取 ` ```json ``` ` 代码块，解析为 `StructuredAnalysisData`
2. **renderStructuredCard**: 渲染四维评分卡片（技术面/基本面/资金面/风险等级）
3. **renderMarkdownSections**: Markdown 分块渲染（降级方案）

### 2.4 当前数据模型

`AIResponseResult` 模型（models.go 行 137-150）：

```go
type AIResponseResult struct {
    gorm.Model
    ChatId    string `json:"chatId"`
    ModelName string `json:"modelName"`
    StockCode string `json:"stockCode"`
    StockName string `json:"stockName"`
    Question  string `json:"question"`
    Content   string `json:"content"`  // 存储完整的 AI 输出
    IsDel     soft_delete.DeletedAt
}
```

---

## 3. 技术方案设计

### 3.1 后端 Prompt 改造

#### 3.1.1 改造目标

设计一个新的系统 Prompt，让 AI 返回结构化 JSON 格式的决策仪表盘数据，同时兼容 go-stock 现有的数据采集逻辑。

#### 3.1.2 新 SYSTEM_PROMPT 完整内容

参考 daily_stock_analysis 的 `analyzer.py`（行 502-686），适配 go-stock 项目场景：

```
你是一位专注于趋势交易的 A 股投资分析师，负责生成专业的【决策仪表盘】分析报告。

## 核心交易理念（必须严格遵守）

### 1. 严进策略（不追高）
- **绝对不追高**：当股价偏离 MA5 超过 5% 时，坚决不买入
- **乖离率公式**：(现价 - MA5) / MA5 × 100%
- 乖离率 < 2%：最佳买点区间
- 乖离率 2-5%：可小仓介入
- 乖离率 > 5%：严禁追高！直接判定为"观望"

### 2. 趋势交易（顺势而为）
- **多头排列必须条件**：MA5 > MA10 > MA20
- 只做多头排列的股票，空头排列坚决不碰
- 均线发散上行优于均线粘合
- 趋势强度判断：看均线间距是否在扩大

### 3. 效率优先（筹码结构）
- 关注筹码集中度：90%集中度 < 15% 表示筹码集中
- 获利比例分析：70-90% 获利盘时需警惕获利回吐
- 平均成本与现价关系：现价高于平均成本 5-15% 为健康

### 4. 买点偏好（回踩支撑）
- **最佳买点**：缩量回踩 MA5 获得支撑
- **次优买点**：回踩 MA10 获得支撑
- **观望情况**：跌破 MA20 时观望

### 5. 风险排查重点
- 减持公告（股东、高管减持）
- 业绩预亏/大幅下滑
- 监管处罚/立案调查
- 行业政策利空
- 大额解禁

### 6. 估值关注（PE/PB）
- 分析时请关注市盈率（PE）是否合理
- PE 明显偏高时（如远超行业平均或历史均值），需在风险点中说明
- 高成长股可适当容忍较高 PE，但需有业绩支撑

## 输出格式：决策仪表盘 JSON

请严格按照以下 JSON 格式输出，这是一个完整的【决策仪表盘】：

```json
{
  "stock_name": "股票中文名称",
  "sentiment_score": 0-100整数,
  "sentiment_label": "极度悲观/悲观/中性/乐观/极度乐观",
  "trend_prediction": "强烈看多/看多/震荡/看空/强烈看空",
  "operation_advice": "买入/加仓/持有/减仓/卖出/观望",
  "decision_type": "buy/hold/sell",
  "confidence_level": "高/中/低",

  "dashboard": {
    "core_conclusion": {
      "one_sentence": "一句话核心结论（30字以内）",
      "signal_type": "🟢买入信号/🟡持有观望/🔴卖出信号/⚠️风险警告",
      "time_sensitivity": "立即行动/今日内/本周内/不急",
      "position_advice": {
        "no_position": "空仓者建议",
        "has_position": "持仓者建议"
      }
    },

    "data_perspective": {
      "trend_status": {
        "ma_alignment": "均线排列状态描述",
        "is_bullish": true,
        "trend_score": 0-100
      },
      "price_position": {
        "current_price": 当前价格,
        "ma5": MA5数值,
        "ma10": MA10数值,
        "ma20": MA20数值,
        "bias_ma5": 乖离率百分比,
        "bias_status": "安全/警戒/危险",
        "support_level": 支撑位,
        "resistance_level": 压力位
      },
      "volume_analysis": {
        "volume_ratio": 量比,
        "volume_status": "放量/缩量/平量",
        "turnover_rate": 换手率,
        "volume_meaning": "量能含义解读"
      },
      "chip_structure": {
        "profit_ratio": 获利比例,
        "avg_cost": 平均成本,
        "concentration": 筹码集中度,
        "chip_health": "健康/一般/警惕"
      }
    },

    "intelligence": {
      "latest_news": "最新消息摘要",
      "risk_alerts": ["风险点1", "风险点2"],
      "positive_catalysts": ["利好1", "利好2"],
      "earnings_outlook": "业绩预期",
      "sentiment_summary": "舆情一句话总结"
    },

    "battle_plan": {
      "sniper_points": {
        "ideal_buy": 理想买入价,
        "secondary_buy": 次优买入价,
        "stop_loss": 止损价,
        "take_profit": 止盈目标价
      },
      "position_strategy": {
        "suggested_position": "建议仓位",
        "entry_plan": "分批建仓策略",
        "risk_control": "风控策略"
      },
      "action_checklist": [
        "✅/⚠️/❌ 检查项1：多头排列",
        "✅/⚠️/❌ 检查项2：乖离率合理",
        "✅/⚠️/❌ 检查项3：量能配合",
        "✅/⚠️/❌ 检查项4：无重大利空",
        "✅/⚠️/❌ 检查项5：筹码健康",
        "✅/⚠️/❌ 检查项6：PE估值合理"
      ]
    }
  },

  "analysis_summary": "100字综合分析摘要",
  "key_points": "3-5个核心看点",
  "risk_warning": "风险提示",
  "buy_reason": "操作理由"
}
```

## 评分标准

### 强烈买入（80-100分）：
- ✅ 多头排列：MA5 > MA10 > MA20
- ✅ 低乖离率：<2%，最佳买点
- ✅ 缩量回调或放量突破
- ✅ 筹码集中健康
- ✅ 消息面有利好催化

### 买入（60-79分）：
- ✅ 多头排列或弱势多头
- ✅ 乖离率 <5%
- ✅ 量能正常
- ⚪ 允许一项次要条件不满足

### 观望（40-59分）：
- ⚠️ 乖离率 >5%（追高风险）
- ⚠️ 均线缠绕趋势不明
- ⚠️ 有风险事件

### 卖出/减仓（0-39分）：
- ❌ 空头排列
- ❌ 跌破MA20
- ❌ 放量下跌
- ❌ 重大利空

## 决策仪表盘核心原则

1. **核心结论先行**：一句话说清该买该卖
2. **分持仓建议**：空仓者和持仓者给不同建议
3. **精确狙击点位**：必须给出具体价格，不说模糊的话
4. **检查清单可视化**：用 ✅⚠️❌ 明确显示每项检查结果
5. **风险优先级**：舆情中的风险点要醒目标出

请基于提供的数据进行分析，严格按照 JSON 格式输出完整的决策仪表盘。
```

#### 3.1.3 新 Prompt 的存储方式

1. **新增 Prompt 模板类型**：在 `prompt_templates` 表中新增类型为 `dashboard` 的 Prompt 模板
2. **默认内置**：系统启动时检查是否存在 `dashboard` 类型的 Prompt，不存在则自动创建默认模板
3. **可配置**：用户可在系统设置中修改 Prompt 内容

#### 3.1.4 改造位置

**backend/data/prompt_template_api.go**：

新增获取仪表盘 Prompt 的方法：

```go
// GetDashboardPrompt 获取决策仪表盘专用 Prompt
func (t PromptTemplateApi) GetDashboardPrompt() string {
    var prompt models.PromptTemplate
    db.Dao.Model(&models.PromptTemplate{}).
        Where("type = ?", "dashboard").
        First(&prompt)
    if prompt.ID == 0 || prompt.Content == "" {
        return DefaultDashboardPrompt // 返回内置默认 Prompt
    }
    return prompt.Content
}

// InitDashboardPrompt 初始化仪表盘 Prompt（启动时调用）
func (t PromptTemplateApi) InitDashboardPrompt() {
    var count int64
    db.Dao.Model(&models.PromptTemplate{}).
        Where("type = ?", "dashboard").
        Count(&count)
    if count == 0 {
        db.Dao.Create(&models.PromptTemplate{
            Name:    "决策仪表盘分析",
            Type:    "dashboard",
            Content: DefaultDashboardPrompt,
        })
    }
}
```

**backend/data/openai_api.go**：

> **v1.1 修正**：原方案直接修改 `NewChatStream` 默认行为（`sysPromptId` 为空时改用仪表盘 Prompt），但这会影响所有使用默认 Prompt 的场景（定时任务 `cron_task_api.go`、Agent `agent_api.go` 等），产生不可控副作用。
>
> **修正方案**：后端 `NewChatStream` 的 Prompt 选择逻辑**不做修改**，保持现有行为。改为**前端主动传递仪表盘 Prompt ID**，由前端在 `buildAnalysisQuestion` 改造时同步传递。

后端只需要：
1. 在 `prompt_template_api.go` 中新增 `GetDashboardPrompt` 和 `InitDashboardPrompt` 方法（如上）
2. `InitDashboardPrompt` 在应用启动时调用，确保仪表盘 Prompt 模板存在
3. 新增 `GetDashboardPromptID` 方法，供前端查询仪表盘 Prompt 的 ID

```go
// GetDashboardPromptID 获取仪表盘 Prompt 的 ID
func (t PromptTemplateApi) GetDashboardPromptID() int {
    var prompt models.PromptTemplate
    db.Dao.Model(&models.PromptTemplate{}).
        Where("type = ?", "dashboard").
        First(&prompt)
    if prompt.ID == 0 {
        // 不存在则初始化并返回
        t.InitDashboardPrompt()
        db.Dao.Model(&models.PromptTemplate{}).
            Where("type = ?", "dashboard").
            First(&prompt)
    }
    return prompt.ID
}
```

`openai_api.go` 的 `NewChatStream` 方法的 Prompt 选择逻辑**保持不变**：

```go
// 保持现有逻辑不变
sysPrompt := ""
if sysPromptId == nil || *sysPromptId == 0 {
    sysPrompt = o.Prompt
} else {
    sysPrompt = NewPromptTemplateApi().GetPromptTemplateByID(*sysPromptId)
}
if sysPrompt == "" {
    sysPrompt = o.Prompt
}
```

**app.go** 中需要新增绑定方法，暴露给前端：

```go
func (a *App) GetDashboardPromptID() int {
    return data.NewPromptTemplateApi().GetDashboardPromptID()
}
```

---

### 3.2 前端仪表盘组件设计

#### 3.2.1 ScoreGauge 组件（新建）

**文件路径**: `frontend/src/app/components/dashboard/ScoreGauge.tsx`

**功能**: SVG 环形进度条，显示情绪评分

**参考**: daily_stock_analysis 的 `ScoreGauge.tsx`

**Props 定义**:

```typescript
interface ScoreGaugeProps {
  score: number;          // 0-100 分数
  size?: 'sm' | 'md' | 'lg';
  showLabel?: boolean;
  className?: string;
}
```

**颜色映射**:
- `score < 40`: fear（红色 #ff4466）
- `score 40-59`: neutral（紫色 #a855f7）
- `score >= 60`: greed（青色 #00d4ff）

**完整实现**:

```tsx
import { useState, useEffect, useRef } from 'react';

interface ScoreGaugeProps {
  score: number;
  size?: 'sm' | 'md' | 'lg';
  showLabel?: boolean;
  className?: string;
}

// 情绪标签
const getSentimentLabel = (score: number): string => {
  if (score <= 20) return '极度悲观';
  if (score <= 40) return '悲观';
  if (score <= 60) return '中性';
  if (score <= 80) return '乐观';
  return '极度乐观';
};

export function ScoreGauge({
  score,
  size = 'md',
  showLabel = true,
  className = '',
}: ScoreGaugeProps) {
  const [animatedScore, setAnimatedScore] = useState(0);
  const [displayScore, setDisplayScore] = useState(0);
  const animationRef = useRef<number | null>(null);
  const prevScoreRef = useRef(0);

  // 动画效果
  useEffect(() => {
    const startScore = prevScoreRef.current;
    const endScore = score;
    const duration = 1000;
    const startTime = performance.now();

    const animate = (currentTime: number) => {
      const elapsed = currentTime - startTime;
      const progress = Math.min(elapsed / duration, 1);
      const easeOut = 1 - Math.pow(1 - progress, 3);
      const currentScore = startScore + (endScore - startScore) * easeOut;
      setAnimatedScore(currentScore);
      setDisplayScore(Math.round(currentScore));

      if (progress < 1) {
        animationRef.current = requestAnimationFrame(animate);
      } else {
        prevScoreRef.current = endScore;
      }
    };

    animationRef.current = requestAnimationFrame(animate);

    return () => {
      if (animationRef.current) {
        cancelAnimationFrame(animationRef.current);
      }
    };
  }, [score]);

  const label = getSentimentLabel(score);

  // 尺寸配置
  const sizeConfig = {
    sm: { width: 100, stroke: 8, fontSize: 'text-2xl', labelSize: 'text-xs' },
    md: { width: 140, stroke: 10, fontSize: 'text-4xl', labelSize: 'text-sm' },
    lg: { width: 180, stroke: 12, fontSize: 'text-5xl', labelSize: 'text-base' },
  };

  const { width, stroke, fontSize, labelSize } = sizeConfig[size];
  const radius = (width - stroke) / 2;
  const circumference = 2 * Math.PI * radius;
  const arcLength = circumference * 0.75;
  const progress = (animatedScore / 100) * arcLength;

  // 颜色配置
  const sentimentConfig = {
    greed: {
      color: '#00d4ff',
      glow: 'rgba(0, 212, 255, 0.4)',
      glowFilter: 'rgba(0, 212, 255, 0.66)',
    },
    neutral: {
      color: '#a855f7',
      glow: 'rgba(168, 85, 247, 0.4)',
      glowFilter: 'rgba(168, 85, 247, 0.66)',
    },
    fear: {
      color: '#ff4466',
      glow: 'rgba(255, 68, 102, 0.4)',
      glowFilter: 'rgba(255, 68, 102, 0.66)',
    },
  };

  const getSentimentKey = (s: number): 'greed' | 'neutral' | 'fear' => {
    if (s >= 60) return 'greed';
    if (s >= 40) return 'neutral';
    return 'fear';
  };

  const sentimentKey = getSentimentKey(animatedScore);
  const colors = sentimentConfig[sentimentKey];
  const uniqueId = `gauge-${sentimentKey}-${score}`;

  return (
    <div className={`flex flex-col items-center ${className}`}>
      {showLabel && (
        <span className="text-xs uppercase tracking-wider text-gray-400 mb-3">
          市场情绪指数
        </span>
      )}

      <div className="relative" style={{ width, height: width }}>
        <svg
          className="overflow-visible"
          width={width}
          height={width}
          style={{ filter: `drop-shadow(0 0 12px ${colors.glowFilter})` }}
        >
          <defs>
            <linearGradient id={`gradient-${uniqueId}`} x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" stopColor={colors.color} stopOpacity="0.6" />
              <stop offset="100%" stopColor={colors.color} stopOpacity="1" />
            </linearGradient>
            <filter id={`glow-${uniqueId}`}>
              <feGaussianBlur stdDeviation="4" result="blur" />
              <feMerge>
                <feMergeNode in="blur" />
                <feMergeNode in="SourceGraphic" />
              </feMerge>
            </filter>
          </defs>

          {/* 背景轨道 */}
          <circle
            cx={width / 2}
            cy={width / 2}
            r={radius}
            fill="none"
            stroke="rgba(255, 255, 255, 0.05)"
            strokeWidth={stroke}
            strokeLinecap="round"
            strokeDasharray={`${arcLength} ${circumference}`}
            transform={`rotate(135 ${width / 2} ${width / 2})`}
          />

          {/* 发光层 */}
          <circle
            cx={width / 2}
            cy={width / 2}
            r={radius}
            fill="none"
            stroke={colors.color}
            strokeWidth={stroke + 6}
            strokeLinecap="round"
            strokeDasharray={`${progress} ${circumference}`}
            transform={`rotate(135 ${width / 2} ${width / 2})`}
            opacity="0.3"
            filter={`url(#glow-${uniqueId})`}
          />

          {/* 进度弧 */}
          <circle
            cx={width / 2}
            cy={width / 2}
            r={radius}
            fill="none"
            stroke={`url(#gradient-${uniqueId})`}
            strokeWidth={stroke}
            strokeLinecap="round"
            strokeDasharray={`${progress} ${circumference}`}
            transform={`rotate(135 ${width / 2} ${width / 2})`}
          />
        </svg>

        {/* 中心数值 */}
        <div className="absolute inset-0 flex flex-col items-center justify-center">
          <span
            className={`font-bold text-white ${fontSize}`}
            style={{ textShadow: `0 0 30px ${colors.glowFilter}` }}
          >
            {displayScore}
          </span>
          {showLabel && (
            <span
              className={`${labelSize} font-semibold mt-1`}
              style={{ color: colors.color }}
            >
              {label}
            </span>
          )}
        </div>
      </div>
    </div>
  );
}
```

#### 3.2.2 DashboardCard 组件（新建）

**文件路径**: `frontend/src/app/components/dashboard/DashboardCard.tsx`

**功能**: 通用卡片容器，支持多种样式变体

```tsx
import { ReactNode } from 'react';

interface DashboardCardProps {
  children: ReactNode;
  variant?: 'default' | 'gradient' | 'bordered';
  className?: string;
  padding?: 'sm' | 'md' | 'lg';
}

export function DashboardCard({
  children,
  variant = 'default',
  className = '',
  padding = 'md',
}: DashboardCardProps) {
  const baseStyles = 'rounded-xl backdrop-blur-xl';
  
  const variantStyles = {
    default: 'bg-slate-900/40 border border-white/10',
    gradient: 'bg-gradient-to-br from-slate-900/60 to-slate-800/40 border border-white/10',
    bordered: 'bg-slate-900/30 border border-cyan-500/20 shadow-lg shadow-cyan-500/5',
  };

  const paddingStyles = {
    sm: 'p-3',
    md: 'p-4',
    lg: 'p-5',
  };

  return (
    <div className={`${baseStyles} ${variantStyles[variant]} ${paddingStyles[padding]} ${className}`}>
      {children}
    </div>
  );
}
```

#### 3.2.3 StrategyPoints 组件（新建）

**文件路径**: `frontend/src/app/components/dashboard/StrategyPoints.tsx`

**功能**: 狙击点位四列网格展示

```tsx
interface SniperPoints {
  ideal_buy?: number | string;
  secondary_buy?: number | string;
  stop_loss?: number | string;
  take_profit?: number | string;
}

interface StrategyPointsProps {
  points?: SniperPoints;
}

interface StrategyItemProps {
  label: string;
  value?: number | string;
  colorVar: string;
}

function StrategyItem({ label, value, colorVar }: StrategyItemProps) {
  const displayValue = value !== undefined && value !== null 
    ? (typeof value === 'number' ? `¥${value.toFixed(2)}` : value)
    : '—';

  return (
    <div className="relative bg-slate-800/40 rounded-lg p-3 border border-white/5">
      <div className="flex flex-col">
        <span className="text-xs text-gray-400 mb-1">{label}</span>
        <span
          className="text-lg font-bold font-mono"
          style={{ color: value ? `var(${colorVar})` : 'rgb(107, 114, 128)' }}
        >
          {displayValue}
        </span>
      </div>
      <div
        className="absolute bottom-0 left-0 right-0 h-0.5 rounded-b-lg"
        style={{ 
          background: value 
            ? `linear-gradient(90deg, transparent, var(${colorVar}), transparent)` 
            : 'transparent' 
        }}
      />
    </div>
  );
}

export function StrategyPoints({ points }: StrategyPointsProps) {
  if (!points) return null;

  const items = [
    { label: '理想买入', value: points.ideal_buy, colorVar: '--color-success' },
    { label: '次优买入', value: points.secondary_buy, colorVar: '--color-info' },
    { label: '止损位', value: points.stop_loss, colorVar: '--color-danger' },
    { label: '止盈目标', value: points.take_profit, colorVar: '--color-warning' },
  ];

  // 定义 CSS 变量
  const cssVars = {
    '--color-success': '#22c55e',
    '--color-info': '#3b82f6',
    '--color-danger': '#ef4444',
    '--color-warning': '#f59e0b',
  } as React.CSSProperties;

  return (
    <div style={cssVars}>
      <div className="flex items-center gap-2 mb-3">
        <span className="text-xs uppercase tracking-wider text-gray-400">STRATEGY POINTS</span>
        <span className="text-sm font-medium text-white">狙击点位</span>
      </div>
      <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
        {items.map((item) => (
          <StrategyItem key={item.label} {...item} />
        ))}
      </div>
    </div>
  );
}
```

#### 3.2.4 ActionChecklist 组件（新建）

**文件路径**: `frontend/src/app/components/dashboard/ActionChecklist.tsx`

**功能**: 操作检查清单列表，解析 ✅/⚠️/❌ 前缀

```tsx
interface ActionChecklistProps {
  items?: string[];
}

type CheckStatus = 'pass' | 'warn' | 'fail' | 'unknown';

function parseCheckItem(item: string): { status: CheckStatus; text: string } {
  if (item.startsWith('✅')) {
    return { status: 'pass', text: item.substring(1).trim() };
  }
  if (item.startsWith('⚠️')) {
    return { status: 'warn', text: item.substring(2).trim() };
  }
  if (item.startsWith('❌')) {
    return { status: 'fail', text: item.substring(1).trim() };
  }
  return { status: 'unknown', text: item };
}

const statusStyles: Record<CheckStatus, { bg: string; border: string; icon: string }> = {
  pass: { 
    bg: 'bg-green-500/10', 
    border: 'border-green-500/30', 
    icon: '✅' 
  },
  warn: { 
    bg: 'bg-yellow-500/10', 
    border: 'border-yellow-500/30', 
    icon: '⚠️' 
  },
  fail: { 
    bg: 'bg-red-500/10', 
    border: 'border-red-500/30', 
    icon: '❌' 
  },
  unknown: { 
    bg: 'bg-gray-500/10', 
    border: 'border-gray-500/30', 
    icon: '•' 
  },
};

export function ActionChecklist({ items }: ActionChecklistProps) {
  if (!items || items.length === 0) return null;

  return (
    <div>
      <div className="flex items-center gap-2 mb-3">
        <span className="text-xs uppercase tracking-wider text-gray-400">CHECKLIST</span>
        <span className="text-sm font-medium text-white">操作检查清单</span>
      </div>
      <div className="grid grid-cols-1 md:grid-cols-2 gap-2">
        {items.map((item, index) => {
          const { status, text } = parseCheckItem(item);
          const styles = statusStyles[status];
          return (
            <div
              key={index}
              className={`flex items-center gap-2 px-3 py-2 rounded-lg border ${styles.bg} ${styles.border}`}
            >
              <span className="text-base">{styles.icon}</span>
              <span className="text-sm text-gray-300">{text}</span>
            </div>
          );
        })}
      </div>
    </div>
  );
}
```

#### 3.2.5 组件索引文件

**文件路径**: `frontend/src/app/components/dashboard/index.ts`

```typescript
export { ScoreGauge } from './ScoreGauge';
export { DashboardCard } from './DashboardCard';
export { StrategyPoints } from './StrategyPoints';
export { ActionChecklist } from './ActionChecklist';
```

#### 3.2.6 AIAnalysis.tsx 改造

**改造要点**:

1. 新增 `DashboardData` 接口定义
2. 新增 `parseDashboardData` 函数
3. 新增 `renderDashboard` 函数
4. 自动检测：有效 JSON → 仪表盘模式，否则 → Markdown 模式
5. 保留展开详情查看原始文本功能

#### 3.2.7 buildAnalysisQuestion 改造（v1.1 新增）

**现有问题**：当前 `buildAnalysisQuestion`（AIAnalysis.tsx 行 255-311）内嵌了旧版 JSON 格式引导，要求 AI 输出 `overallScore/recommendation/technicalScore` 等字段。这与仪表盘系统 Prompt 中的 JSON 格式冲突。

**改造方案**：简化 `buildAnalysisQuestion`，去掉内嵌的 JSON 格式引导（格式由系统 Prompt 统一控制），同时前端在调用 `NewChatStream` 时传递仪表盘 Prompt 的 ID。

**改造后的 buildAnalysisQuestion**：

```typescript
const buildAnalysisQuestion = (stockName: string, stockCode: string): string => {
  return `请对 ${stockName}(${stockCode}) 进行全面深度分析，输出完整的决策仪表盘 JSON。

请基于系统提供的实时行情数据、K线数据、财务数据、新闻资讯等进行分析，严格按照系统提示中要求的 JSON 格式输出。所有评分和价格必须基于实际数据给出合理数值，不要编造不存在的数据。`;
};
```

**调用改造**：`startSingleAnalysis` 中调用 `NewChatStream` 时，传递仪表盘 Prompt ID：

```typescript
// 在组件初始化时获取仪表盘 Prompt ID
const [dashboardPromptId, setDashboardPromptId] = useState<number | null>(null);

useEffect(() => {
  GetDashboardPromptID().then(id => setDashboardPromptId(id));
}, []);

// 调用时传递 Prompt ID
NewChatStream(
  stockName,
  stockCode,
  question,
  selectedAiConfigId,
  dashboardPromptId,  // 传递仪表盘 Prompt ID，替代原来的 null
  true,
  false
);
```

**注意**：前端需要从 wailsjs 绑定中导入新增的 `GetDashboardPromptID` 方法。

#### 3.2.8 流式解析时机说明（v1.1 新增）

**现有行为**：流式过程中每个片段到达时都调用 `parseStructuredData` 解析 JSON，这对旧格式（小型 JSON + Markdown）尚可接受。

**仪表盘模式调整**：仪表盘 JSON 体积较大，流式中间状态的 JSON 不完整，频繁解析无意义且浪费性能。

**改造策略**：
1. **流式过程中**：只累积文本到 `content`，不尝试解析仪表盘 JSON（可保留旧的 `parseStructuredData` 做 Markdown 分割以显示进度文本）
2. **流式完成后（DONE 事件）**：在完整内容上调用 `parseDashboardData`，若成功则设置 `dashboardData`，否则回退到旧的 `parseStructuredData` + Markdown 模式
3. **历史记录加载时**：同样先尝试 `parseDashboardData`，再尝试旧的 `parseStructuredData`

---

## 4. 数据结构定义

### 4.1 TypeScript 接口定义

在 AIAnalysis.tsx 中新增：

```typescript
// ============ 决策仪表盘数据结构 ============

interface PositionAdvice {
  no_position?: string;
  has_position?: string;
}

interface CoreConclusion {
  one_sentence?: string;
  signal_type?: string;
  time_sensitivity?: string;
  position_advice?: PositionAdvice;
}

interface TrendStatus {
  ma_alignment?: string;
  is_bullish?: boolean;
  trend_score?: number;
}

interface PricePosition {
  current_price?: number;
  ma5?: number;
  ma10?: number;
  ma20?: number;
  bias_ma5?: number;
  bias_status?: string;
  support_level?: number;
  resistance_level?: number;
}

interface VolumeAnalysis {
  volume_ratio?: number;
  volume_status?: string;
  turnover_rate?: number;
  volume_meaning?: string;
}

interface ChipStructure {
  profit_ratio?: number;
  avg_cost?: number;
  concentration?: number;
  chip_health?: string;
}

interface DataPerspective {
  trend_status?: TrendStatus;
  price_position?: PricePosition;
  volume_analysis?: VolumeAnalysis;
  chip_structure?: ChipStructure;
}

interface Intelligence {
  latest_news?: string;
  risk_alerts?: string[];
  positive_catalysts?: string[];
  earnings_outlook?: string;
  sentiment_summary?: string;
}

interface SniperPoints {
  ideal_buy?: number | string;
  secondary_buy?: number | string;
  stop_loss?: number | string;
  take_profit?: number | string;
}

interface PositionStrategy {
  suggested_position?: string;
  entry_plan?: string;
  risk_control?: string;
}

interface BattlePlan {
  sniper_points?: SniperPoints;
  position_strategy?: PositionStrategy;
  action_checklist?: string[];
}

interface Dashboard {
  core_conclusion?: CoreConclusion;
  data_perspective?: DataPerspective;
  intelligence?: Intelligence;
  battle_plan?: BattlePlan;
}

interface DashboardData {
  stock_name?: string;
  sentiment_score?: number;
  sentiment_label?: string;
  trend_prediction?: string;
  operation_advice?: string;
  decision_type?: 'buy' | 'hold' | 'sell';
  confidence_level?: string;
  dashboard?: Dashboard;
  analysis_summary?: string;
  key_points?: string;
  risk_warning?: string;
  buy_reason?: string;
}
```

### 4.2 数据解析函数

```typescript
/**
 * 解析仪表盘数据
 * 尝试从 AI 返回内容中提取 JSON 格式的仪表盘数据
 */
function parseDashboardData(content: string): {
  dashboardData: DashboardData | null;
  rawContent: string;
} {
  if (!content) {
    return { dashboardData: null, rawContent: content };
  }

  // 尝试提取 JSON 代码块
  let jsonStr = content;
  
  // 方式1：提取 ```json ``` 代码块
  const jsonBlockRegex = /```json\s*([\s\S]*?)```/;
  const blockMatch = content.match(jsonBlockRegex);
  if (blockMatch) {
    jsonStr = blockMatch[1].trim();
  } else {
    // 方式2：直接查找 JSON 对象
    const jsonStart = content.indexOf('{');
    const jsonEnd = content.lastIndexOf('}');
    if (jsonStart >= 0 && jsonEnd > jsonStart) {
      jsonStr = content.substring(jsonStart, jsonEnd + 1);
    }
  }

  try {
    // 修复常见 JSON 问题
    jsonStr = jsonStr
      .replace(/,\s*}/g, '}')  // 移除尾随逗号
      .replace(/,\s*]/g, ']')
      .replace(/True/g, 'true')
      .replace(/False/g, 'false');

    const parsed = JSON.parse(jsonStr);

    // 验证是否为有效的仪表盘数据
    if (
      typeof parsed.sentiment_score === 'number' &&
      parsed.dashboard &&
      typeof parsed.dashboard === 'object'
    ) {
      return {
        dashboardData: parsed as DashboardData,
        rawContent: content,
      };
    }
  } catch (e) {
    // JSON 解析失败，返回 null
  }

  return { dashboardData: null, rawContent: content };
}
```

---

## 5. 涉及文件清单

### 5.1 新建文件

| 文件路径 | 说明 |
|---------|------|
| `frontend/src/app/components/dashboard/ScoreGauge.tsx` | 情绪仪表盘 SVG 组件 |
| `frontend/src/app/components/dashboard/DashboardCard.tsx` | 通用卡片容器组件 |
| `frontend/src/app/components/dashboard/StrategyPoints.tsx` | 狙击点位组件 |
| `frontend/src/app/components/dashboard/ActionChecklist.tsx` | 操作检查清单组件 |
| `frontend/src/app/components/dashboard/index.ts` | 组件导出索引 |

### 5.2 修改文件

| 文件路径 | 改动要点 |
|---------|---------|
| `frontend/src/app/components/AIAnalysis.tsx` | 新增 DashboardData 接口、parseDashboardData 函数、renderDashboard 函数；修改 StockAnalysisState 接口增加 dashboardData 字段；改造 `buildAnalysisQuestion` 去掉旧版 JSON 引导；改造 `startSingleAnalysis` 传递仪表盘 Prompt ID；修改流式完成后的解析逻辑 |
| `backend/data/prompt_template_api.go` | 新增 GetDashboardPrompt、InitDashboardPrompt、GetDashboardPromptID 方法；新增 DefaultDashboardPrompt 常量 |
| `app.go` | 新增 GetDashboardPromptID 绑定方法，暴露给前端调用 |

> **v1.1 修正**：`backend/data/openai_api.go` 不再需要修改，Prompt 选择逻辑保持不变，由前端传递 sysPromptId 来控制。

---

## 6. 跨域协议

### 6.1 前后端数据交互格式

**事件格式不变**：使用 `newChatStream` 事件进行流式传输

```typescript
// 后端 → 前端事件格式
interface StreamMessage {
  chatId?: string;
  content?: string;      // 流式内容片段
  extraContent?: string; // 额外内容（数据采集结果等）
}

// 流结束标识
"DONE"
```

### 6.2 JSON 提取策略

前端在流式接收完成后，从完整的 `Content` 中提取 JSON：

1. 优先提取 ` ```json ``` ` 代码块中的内容
2. 若无代码块，尝试查找 `{...}` 范围
3. 解析成功且包含 `sentiment_score` + `dashboard` 字段 → 使用仪表盘模式
4. 否则 → 尝试旧版 `parseStructuredData`（兼容历史数据）
5. 均失败 → 使用 Markdown 降级模式

### 6.3 解析时机（v1.1 新增）

| 阶段 | 解析行为 |
|------|---------|
| 流式接收中（每个片段） | **不解析**仪表盘 JSON；仅累积原始文本到 `content`，可显示"分析中..."状态 |
| 流式完成（DONE 事件） | 在完整 `content` 上依次尝试：`parseDashboardData` → `parseStructuredData` → Markdown 降级 |
| 加载历史记录 | 同 DONE 逻辑：先尝试仪表盘解析，再尝试旧格式，最后 Markdown |

---

## 7. 开发步骤

### 步骤 1：定义 TypeScript 接口和数据解析函数

**做什么**：在 AIAnalysis.tsx 中新增仪表盘数据结构定义和解析函数

**改哪些文件**：
- `frontend/src/app/components/AIAnalysis.tsx`

**依赖关系**：无

**验证方式**：TypeScript 编译无报错

### 步骤 2：新建 ScoreGauge 组件

**做什么**：创建 SVG 环形进度条组件

**改哪些文件**：
- 新建 `frontend/src/app/components/dashboard/ScoreGauge.tsx`

**依赖关系**：步骤 1 完成

**验证方式**：
```tsx
// 测试渲染
<ScoreGauge score={75} size="lg" />
```

### 步骤 3：新建 DashboardCard、StrategyPoints、ActionChecklist 组件

**做什么**：创建仪表盘子组件

**改哪些文件**：
- 新建 `frontend/src/app/components/dashboard/DashboardCard.tsx`
- 新建 `frontend/src/app/components/dashboard/StrategyPoints.tsx`
- 新建 `frontend/src/app/components/dashboard/ActionChecklist.tsx`
- 新建 `frontend/src/app/components/dashboard/index.ts`

**依赖关系**：步骤 2 完成

**验证方式**：组件可正常导入和渲染

### 步骤 4：改造 AIAnalysis.tsx 集成仪表盘展示

**做什么**：
1. 修改 `StockAnalysisState` 接口，增加 `dashboardData` 字段
2. 新增 `renderDashboard` 函数（含 position_advice、data_perspective、position_strategy、key_points 展示）
3. 改造 `buildAnalysisQuestion` 函数，去掉旧版 JSON 格式引导，简化为引导 AI 按系统 Prompt 要求输出
4. 改造 `startSingleAnalysis`，获取并传递仪表盘 Prompt ID 到 `NewChatStream` 调用
5. 修改流式完成后的数据解析逻辑：DONE 事件中依次尝试 `parseDashboardData` → `parseStructuredData` → Markdown
6. 修改渲染逻辑：有 dashboardData → 仪表盘模式，否则 → Markdown 模式
7. 从 wailsjs 绑定导入新增的 `GetDashboardPromptID` 方法

**改哪些文件**：
- `frontend/src/app/components/AIAnalysis.tsx`

**依赖关系**：步骤 3 完成

**验证方式**：
1. 使用现有历史数据，确保 Markdown 模式正常渲染（兼容旧格式）
2. 模拟仪表盘 JSON 数据，验证仪表盘模式渲染

### 步骤 5：后端 Prompt 改造

**做什么**：
1. 在 `prompt_template_api.go` 中新增 `GetDashboardPrompt`、`InitDashboardPrompt`、`GetDashboardPromptID` 方法和 `DefaultDashboardPrompt` 常量
2. 在 `app.go` 中新增 `GetDashboardPromptID` 绑定方法
3. 在应用启动流程中调用 `InitDashboardPrompt` 确保仪表盘 Prompt 模板存在
4. **不修改** `openai_api.go` 的 Prompt 选择逻辑（由前端传 sysPromptId 控制）

**改哪些文件**：
- `backend/data/prompt_template_api.go`
- `app.go`

**依赖关系**：步骤 4 完成

**验证方式**：
1. 启动后端，检查 `prompt_templates` 表中 `dashboard` 类型记录是否自动创建
2. 前端调用 `GetDashboardPromptID()` 能返回有效 ID
3. 通过 AI 分析流程验证 Prompt 被正确使用，返回仪表盘 JSON

### 步骤 6：前端构建验证

**做什么**：
1. 执行 `npm run build` 确保无编译错误
2. 执行完整的 AI 分析流程测试
3. 验证新旧数据兼容性

**改哪些文件**：无

**依赖关系**：步骤 5 完成

**验证方式**：
1. 新分析结果以仪表盘形式展示
2. 历史纯文本记录以 Markdown 方式正常展示
3. 情绪仪表盘动画正常
4. 狙击点位显示具体价格
5. 检查清单可视化展示
6. 前端构建无报错

---

## 附录 A：renderDashboard 函数完整实现

```tsx
const renderDashboard = (stock: StockAnalysisState) => {
  const { dashboardData } = stock;
  if (!dashboardData) return null;

  const dashboard = dashboardData.dashboard;
  const coreConclusion = dashboard?.core_conclusion;
  const dataPerspective = dashboard?.data_perspective;
  const intelligence = dashboard?.intelligence;
  const battlePlan = dashboard?.battle_plan;

  // 操作建议样式
  const getAdviceStyle = (advice?: string) => {
    const styles: Record<string, { bg: string; text: string; border: string }> = {
      '买入': { bg: 'bg-green-500/20', text: 'text-green-400', border: 'border-green-500/30' },
      '加仓': { bg: 'bg-green-500/20', text: 'text-green-400', border: 'border-green-500/30' },
      '持有': { bg: 'bg-yellow-500/20', text: 'text-yellow-400', border: 'border-yellow-500/30' },
      '观望': { bg: 'bg-gray-500/20', text: 'text-gray-400', border: 'border-gray-500/30' },
      '减仓': { bg: 'bg-orange-500/20', text: 'text-orange-400', border: 'border-orange-500/30' },
      '卖出': { bg: 'bg-red-500/20', text: 'text-red-400', border: 'border-red-500/30' },
    };
    return styles[advice || ''] || styles['观望'];
  };

  const adviceStyle = getAdviceStyle(dashboardData.operation_advice);

  return (
    <div className="space-y-4">
      {/* 头部信息区 + 情绪仪表盘 */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-4">
        {/* 左侧：股票信息与结论 */}
        <div className="lg:col-span-2 space-y-4">
          {/* 股票头部 */}
          <DashboardCard variant="gradient">
            <div className="flex items-start justify-between mb-4">
              <div>
                <div className="flex items-center gap-3">
                  <h3 className="text-2xl font-bold text-white">
                    {dashboardData.stock_name || stock.stockName}
                  </h3>
                  <span className="text-sm text-gray-400 font-mono">{stock.stockCode}</span>
                  <span className={`px-2 py-0.5 rounded text-xs font-medium ${adviceStyle.bg} ${adviceStyle.text} border ${adviceStyle.border}`}>
                    {dashboardData.operation_advice || '观望'}
                  </span>
                </div>
                <p className="text-xs text-gray-500 mt-1">{stock.updatedAt} 更新</p>
              </div>
            </div>

            {/* 核心结论 */}
            {coreConclusion && (
              <div className="border-t border-white/10 pt-4">
                <span className="text-xs uppercase tracking-wider text-gray-400">KEY INSIGHTS</span>
                <p className="mt-2 text-gray-200 leading-relaxed">
                  {coreConclusion.one_sentence || dashboardData.analysis_summary}
                </p>
                {coreConclusion.signal_type && (
                  <div className="mt-3 flex items-center gap-2">
                    <span className="text-lg">{coreConclusion.signal_type.split('/')[0]}</span>
                    <span className="text-sm text-gray-400">{coreConclusion.time_sensitivity}</span>
                  </div>
                )}

                {/* v1.1 新增：持仓建议 */}
                {coreConclusion.position_advice && (
                  <div className="mt-3 grid grid-cols-2 gap-3">
                    <div className="bg-blue-500/10 rounded-lg p-2 border border-blue-500/20">
                      <span className="text-xs text-blue-400">空仓者</span>
                      <p className="text-sm text-gray-300 mt-1">{coreConclusion.position_advice.no_position}</p>
                    </div>
                    <div className="bg-purple-500/10 rounded-lg p-2 border border-purple-500/20">
                      <span className="text-xs text-purple-400">持仓者</span>
                      <p className="text-sm text-gray-300 mt-1">{coreConclusion.position_advice.has_position}</p>
                    </div>
                  </div>
                )}
              </div>
            )}
          </DashboardCard>

          {/* 操作建议和趋势预测 */}
          <div className="grid grid-cols-2 gap-3">
            <DashboardCard variant="bordered" padding="sm">
              <div className="flex items-start gap-3">
                <div className="w-8 h-8 rounded-lg bg-green-500/10 flex items-center justify-center">
                  <Target className="w-4 h-4 text-green-400" />
                </div>
                <div>
                  <h4 className="text-xs font-medium uppercase tracking-wider text-green-400">操作建议</h4>
                  <p className="text-sm text-gray-200 mt-1">{dashboardData.operation_advice || '—'}</p>
                </div>
              </div>
            </DashboardCard>

            <DashboardCard variant="bordered" padding="sm">
              <div className="flex items-start gap-3">
                <div className="w-8 h-8 rounded-lg bg-yellow-500/10 flex items-center justify-center">
                  <TrendingUp className="w-4 h-4 text-yellow-400" />
                </div>
                <div>
                  <h4 className="text-xs font-medium uppercase tracking-wider text-yellow-400">趋势预测</h4>
                  <p className="text-sm text-gray-200 mt-1">{dashboardData.trend_prediction || '—'}</p>
                </div>
              </div>
            </DashboardCard>
          </div>
        </div>

        {/* 右侧：情绪仪表盘 */}
        <div className="flex flex-col">
          <DashboardCard variant="bordered" className="flex-1 flex items-center justify-center">
            <ScoreGauge 
              score={dashboardData.sentiment_score || 50} 
              size="lg" 
            />
          </DashboardCard>
        </div>
      </div>

      {/* v1.1 新增：数据视角 - 趋势/价格/量能/筹码 */}
      {dataPerspective && (
        <DashboardCard variant="bordered">
          <div className="flex items-center gap-2 mb-3">
            <span className="text-xs uppercase tracking-wider text-gray-400">DATA PERSPECTIVE</span>
            <span className="text-sm font-medium text-white">数据视角</span>
          </div>
          <div className="grid grid-cols-2 lg:grid-cols-4 gap-3">
            {/* 趋势状态 */}
            {dataPerspective.trend_status && (
              <div className="bg-slate-800/40 rounded-lg p-3 border border-white/5">
                <span className="text-xs text-gray-400">趋势状态</span>
                <p className={`text-sm font-medium mt-1 ${dataPerspective.trend_status.is_bullish ? 'text-green-400' : 'text-red-400'}`}>
                  {dataPerspective.trend_status.ma_alignment || '—'}
                </p>
                {dataPerspective.trend_status.trend_score !== undefined && (
                  <div className="mt-2 h-1.5 bg-slate-700 rounded-full overflow-hidden">
                    <div
                      className="h-full rounded-full transition-all"
                      style={{
                        width: `${dataPerspective.trend_status.trend_score}%`,
                        backgroundColor: dataPerspective.trend_status.is_bullish ? '#22c55e' : '#ef4444',
                      }}
                    />
                  </div>
                )}
              </div>
            )}
            {/* 价格位置 */}
            {dataPerspective.price_position && (
              <div className="bg-slate-800/40 rounded-lg p-3 border border-white/5">
                <span className="text-xs text-gray-400">价格位置</span>
                <p className="text-sm font-mono text-white mt-1">
                  ¥{dataPerspective.price_position.current_price}
                </p>
                <div className="mt-1 flex items-center gap-1">
                  <span className="text-xs text-gray-500">乖离</span>
                  <span className={`text-xs font-mono ${
                    dataPerspective.price_position.bias_status === '安全' ? 'text-green-400' :
                    dataPerspective.price_position.bias_status === '警戒' ? 'text-yellow-400' : 'text-red-400'
                  }`}>
                    {dataPerspective.price_position.bias_ma5 !== undefined ? `${dataPerspective.price_position.bias_ma5}%` : '—'}
                  </span>
                </div>
              </div>
            )}
            {/* 量能分析 */}
            {dataPerspective.volume_analysis && (
              <div className="bg-slate-800/40 rounded-lg p-3 border border-white/5">
                <span className="text-xs text-gray-400">量能分析</span>
                <p className="text-sm font-medium text-white mt-1">
                  {dataPerspective.volume_analysis.volume_status || '—'}
                </p>
                <p className="text-xs text-gray-500 mt-1">
                  量比 {dataPerspective.volume_analysis.volume_ratio ?? '—'} · 换手 {dataPerspective.volume_analysis.turnover_rate !== undefined ? `${dataPerspective.volume_analysis.turnover_rate}%` : '—'}
                </p>
              </div>
            )}
            {/* 筹码结构 */}
            {dataPerspective.chip_structure && (
              <div className="bg-slate-800/40 rounded-lg p-3 border border-white/5">
                <span className="text-xs text-gray-400">筹码结构</span>
                <p className={`text-sm font-medium mt-1 ${
                  dataPerspective.chip_structure.chip_health === '健康' ? 'text-green-400' :
                  dataPerspective.chip_structure.chip_health === '一般' ? 'text-yellow-400' : 'text-red-400'
                }`}>
                  {dataPerspective.chip_structure.chip_health || '—'}
                </p>
                <p className="text-xs text-gray-500 mt-1">
                  获利 {dataPerspective.chip_structure.profit_ratio !== undefined ? `${dataPerspective.chip_structure.profit_ratio}%` : '—'}
                </p>
              </div>
            )}
          </div>
        </DashboardCard>
      )}

      {/* 狙击点位 */}
      {battlePlan?.sniper_points && (
        <DashboardCard variant="bordered">
          <StrategyPoints points={battlePlan.sniper_points} />
        </DashboardCard>
      )}

      {/* 操作检查清单 */}
      {battlePlan?.action_checklist && battlePlan.action_checklist.length > 0 && (
        <DashboardCard variant="bordered">
          <ActionChecklist items={battlePlan.action_checklist} />
        </DashboardCard>
      )}

      {/* v1.1 新增：仓位策略 */}
      {battlePlan?.position_strategy && (
        <DashboardCard variant="bordered">
          <div className="flex items-center gap-2 mb-3">
            <span className="text-xs uppercase tracking-wider text-gray-400">POSITION STRATEGY</span>
            <span className="text-sm font-medium text-white">仓位策略</span>
          </div>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-3">
            <div className="bg-slate-800/40 rounded-lg p-3 border border-white/5">
              <span className="text-xs text-gray-400">建议仓位</span>
              <p className="text-sm text-white mt-1">{battlePlan.position_strategy.suggested_position || '—'}</p>
            </div>
            <div className="bg-slate-800/40 rounded-lg p-3 border border-white/5">
              <span className="text-xs text-gray-400">建仓策略</span>
              <p className="text-sm text-white mt-1">{battlePlan.position_strategy.entry_plan || '—'}</p>
            </div>
            <div className="bg-slate-800/40 rounded-lg p-3 border border-white/5">
              <span className="text-xs text-gray-400">风控策略</span>
              <p className="text-sm text-white mt-1">{battlePlan.position_strategy.risk_control || '—'}</p>
            </div>
          </div>
        </DashboardCard>
      )}

      {/* v1.1 新增：核心看点与风险提示 */}
      {(dashboardData.key_points || dashboardData.risk_warning || dashboardData.buy_reason) && (
        <DashboardCard variant="bordered">
          <div className="flex items-center gap-2 mb-3">
            <span className="text-xs uppercase tracking-wider text-gray-400">KEY POINTS</span>
            <span className="text-sm font-medium text-white">核心看点</span>
          </div>
          <div className="space-y-2">
            {dashboardData.key_points && (
              <p className="text-sm text-gray-300">{dashboardData.key_points}</p>
            )}
            {dashboardData.buy_reason && (
              <div className="flex items-start gap-2">
                <span className="text-xs text-cyan-400 shrink-0 mt-0.5">操作理由</span>
                <p className="text-sm text-gray-300">{dashboardData.buy_reason}</p>
              </div>
            )}
            {dashboardData.risk_warning && (
              <div className="flex items-start gap-2">
                <span className="text-xs text-red-400 shrink-0 mt-0.5">风险提示</span>
                <p className="text-sm text-gray-400">{dashboardData.risk_warning}</p>
              </div>
            )}
          </div>
        </DashboardCard>
      )}

      {/* 舆情情报 */}
      {intelligence && (
        <DashboardCard variant="bordered">
          <div className="flex items-center gap-2 mb-3">
            <span className="text-xs uppercase tracking-wider text-gray-400">NEWS FEED</span>
            <span className="text-sm font-medium text-white">舆情情报</span>
          </div>
          <div className="space-y-3">
            {intelligence.latest_news && (
              <p className="text-sm text-gray-300">{intelligence.latest_news}</p>
            )}
            {intelligence.risk_alerts && intelligence.risk_alerts.length > 0 && (
              <div>
                <span className="text-xs text-red-400">风险提示：</span>
                <ul className="mt-1 space-y-1">
                  {intelligence.risk_alerts.map((alert, i) => (
                    <li key={i} className="text-sm text-gray-400 flex items-start gap-2">
                      <span className="text-red-400">•</span>
                      {alert}
                    </li>
                  ))}
                </ul>
              </div>
            )}
            {intelligence.positive_catalysts && intelligence.positive_catalysts.length > 0 && (
              <div>
                <span className="text-xs text-green-400">利好因素：</span>
                <ul className="mt-1 space-y-1">
                  {intelligence.positive_catalysts.map((catalyst, i) => (
                    <li key={i} className="text-sm text-gray-400 flex items-start gap-2">
                      <span className="text-green-400">•</span>
                      {catalyst}
                    </li>
                  ))}
                </ul>
              </div>
            )}
          </div>
        </DashboardCard>
      )}
    </div>
  );
};
```

---

## 附录 B：后端 DefaultDashboardPrompt 常量

```go
const DefaultDashboardPrompt = `你是一位专注于趋势交易的 A 股投资分析师，负责生成专业的【决策仪表盘】分析报告。

## 核心交易理念（必须严格遵守）

### 1. 严进策略（不追高）
- **绝对不追高**：当股价偏离 MA5 超过 5% 时，坚决不买入
- **乖离率公式**：(现价 - MA5) / MA5 × 100%
- 乖离率 < 2%：最佳买点区间
- 乖离率 2-5%：可小仓介入
- 乖离率 > 5%：严禁追高！直接判定为"观望"

### 2. 趋势交易（顺势而为）
- **多头排列必须条件**：MA5 > MA10 > MA20
- 只做多头排列的股票，空头排列坚决不碰

### 3. 风险排查重点
- 减持公告、业绩预亏、监管处罚、行业利空、大额解禁

## 输出格式

请严格按照以下 JSON 格式输出决策仪表盘：

` + "```json" + `
{
  "stock_name": "股票中文名称",
  "sentiment_score": 0-100整数,
  "sentiment_label": "极度悲观/悲观/中性/乐观/极度乐观",
  "trend_prediction": "强烈看多/看多/震荡/看空/强烈看空",
  "operation_advice": "买入/加仓/持有/减仓/卖出/观望",
  "decision_type": "buy/hold/sell",
  "confidence_level": "高/中/低",
  "dashboard": {
    "core_conclusion": {
      "one_sentence": "一句话核心结论（30字以内）",
      "signal_type": "🟢买入信号/🟡持有观望/🔴卖出信号/⚠️风险警告",
      "time_sensitivity": "立即行动/今日内/本周内/不急",
      "position_advice": {
        "no_position": "空仓者建议",
        "has_position": "持仓者建议"
      }
    },
    "data_perspective": {
      "trend_status": {
        "ma_alignment": "均线排列状态",
        "is_bullish": true,
        "trend_score": 0-100
      },
      "price_position": {
        "current_price": 当前价格,
        "ma5": MA5,
        "ma10": MA10,
        "ma20": MA20,
        "bias_ma5": 乖离率,
        "bias_status": "安全/警戒/危险",
        "support_level": 支撑位,
        "resistance_level": 压力位
      },
      "volume_analysis": {
        "volume_ratio": 量比,
        "volume_status": "放量/缩量/平量",
        "turnover_rate": 换手率,
        "volume_meaning": "量能解读"
      },
      "chip_structure": {
        "profit_ratio": 获利比例,
        "avg_cost": 平均成本,
        "concentration": 筹码集中度,
        "chip_health": "健康/一般/警惕"
      }
    },
    "intelligence": {
      "latest_news": "最新消息摘要",
      "risk_alerts": ["风险点1", "风险点2"],
      "positive_catalysts": ["利好1", "利好2"],
      "earnings_outlook": "业绩预期",
      "sentiment_summary": "舆情总结"
    },
    "battle_plan": {
      "sniper_points": {
        "ideal_buy": 理想买入价,
        "secondary_buy": 次优买入价,
        "stop_loss": 止损价,
        "take_profit": 止盈目标价
      },
      "position_strategy": {
        "suggested_position": "建议仓位",
        "entry_plan": "建仓策略",
        "risk_control": "风控策略"
      },
      "action_checklist": [
        "✅/⚠️/❌ 检查项1：多头排列",
        "✅/⚠️/❌ 检查项2：乖离率合理",
        "✅/⚠️/❌ 检查项3：量能配合",
        "✅/⚠️/❌ 检查项4：无重大利空",
        "✅/⚠️/❌ 检查项5：筹码健康",
        "✅/⚠️/❌ 检查项6：PE估值合理"
      ]
    }
  },
  "analysis_summary": "100字综合分析摘要",
  "key_points": "核心看点",
  "risk_warning": "风险提示",
  "buy_reason": "操作理由"
}
` + "```" + `

## 评分标准

- 80-100分：强烈买入（多头排列+低乖离率+利好催化）
- 60-79分：买入（满足大部分条件）
- 40-59分：观望（乖离率>5%或趋势不明）
- 0-39分：卖出/减仓（空头排列或重大利空）

请基于提供的数据进行分析，严格按照 JSON 格式输出。`
```

---

## 附录 C：v1.1 补全项汇总

| 补全项 | 涉及位置 | 说明 |
|-------|---------|------|
| `buildAnalysisQuestion` 改造 | 3.2.7、步骤 4 | 去掉旧版 JSON 引导，简化为系统 Prompt 统一控制格式 |
| Prompt 使用策略修正 | 3.1.4、步骤 5 | 不修改后端默认行为，前端主动传递仪表盘 Prompt ID |
| `GetDashboardPromptID` 方法 | 3.1.4、5.2 | 后端新增方法 + app.go 绑定，供前端获取 Prompt ID |
| 流式解析时机 | 3.2.8、6.3 | 流式中不解析仪表盘 JSON，DONE 后统一解析 |
| `position_advice` 展示 | 附录 A | 核心结论区新增空仓者/持仓者建议展示 |
| `data_perspective` 展示 | 附录 A | 新增数据视角模块（趋势/价格/量能/筹码） |
| `position_strategy` 展示 | 附录 A | 新增仓位策略模块（建议仓位/建仓策略/风控策略） |
| `key_points` / `risk_warning` / `buy_reason` 展示 | 附录 A | 新增核心看点与风险提示模块 |

---

## 文档结束
