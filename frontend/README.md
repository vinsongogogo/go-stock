
  # Upload Image Feature

  This is a code bundle for Upload Image Feature. The original project is available at https://www.figma.com/design/uiehWNFzzWLiEOaPKDnsSF/Upload-Image-Feature.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  